---
title: "Send verifications by email"
description: "Import the people you already have and send each one their own verification link by email."
icon: "envelope"
---

{/*
WRITTEN 2026-08-28 from James's live walkthrough of the flow (five annotated screenshots of the
kindly-dormouse verification on platform.verifyyou.com) plus the 21 Aug dashboard scan in
DASHBOARD_REFERENCE.md, which carries the Add people panel copy and the Switch method dialog
verbatim. Format mirrors the four one-time-password guides exactly, at James's request.

UI facts taken from the screenshots, not memory: the Senders panel ("The addresses your invites
leave from", Add sender, "Confirmed. Ready to send", the TXT and CNAME rows with Live/Pending
and Re-check, "Publish these records to send from your own domain (optional)"); the send dialog
("Send to 1 people?", "Each person gets a verification link locked to their address",
Recipients / From / Message with the Initial invite dropdown and Edit); the Activity header
(the address chip beside Send unsent, the sent-today counter).

The default invite copy in the send dialog uses a phrase the claim gates ban, so the guide
describes the message rather than quoting it. Same reason this page never quotes the test
panel.

The daily send cap: the Activity header shows a sent-today counter on this workspace. Per the
integration.mdx note, the number may be per plan, so the Note points at the counter and prints
no number. Confirm with Zeek before ever printing one.

STEP 3 REWRITTEN 2026-09-09 for the September Map columns ship, then VERIFIED THE SAME DAY by
walking the flow in the livetest workspace (genial-narwhal, method switched to Existing users
and back, a two-row example.com CSV staged and backed out of, nothing imported or sent).
Observed and now stated exactly: upload parses first ("2 people ready to add - we also picked
up their IP address"), Map columns opens on clicking Add, not at upload; the dialog is titled
"Map columns / Match your columns to the critical fields" with a banner like "Email and IP
matched - 1 extra column found"; critical fields are FOUR, Email address plus optional IP
address, Impact figure and Date for the figure (the changelog said three; the date field is
real); your columns are the dropdowns on the left; extra data reads "saved to each person's
record" with an Import as / Type table and a "Select columns to leave out" tick, so the old
"untick to skip" wording was inverted; footer reads "Importing 4 critical fields + 1 extra
column" and the confirm button is "Confirm mapping". Type chips seen: Email, IP, currency
(a dropdown), Date, Text; the currency/date/number/text preset list stays on the changelog's
authority. Skipped-row counting and empty-cells behaviour also stay on the changelog's
authority, the test CSV had no gaps to show them.

RETITLED AND REVERIFIED 2026-09-17, James's ask, title exactly as he dictated it. The slug and
URL are deliberately unchanged: the link is being sent around in email. Walked live prod that
day on ineffable-dormouse (0 people; nothing imported, sent, or saved). What changed on the
page as a result:
  - The import buttons are "Upload CSV" and "Add people". The old "Add" wording is gone, and
    the panel helper copy no longer carries the August auto-detect sentence.
  - The Activity tab is the entry to sending now. Its header carries the person count, a
    Message control, the sender address chip, and "Send unsent" with a count. Step 4's old
    entry (Recently added, then View all) was replaced with the tab itself.
  - Message opens a Messages modal: two named types (Initial invite, "Not yet verified";
    Re-verification, "Already verified"), a subject and rich body per type, a toolbar with a
    Verification link token whose helper says each one becomes that person's own link, and a
    footer saying changes apply to all future sends of this message. The template body on the
    live verification is a real team's copy, so nothing from it is quoted here.
  - Senders panel is unchanged from the August scan (Add sender, "Confirmed. Ready to send",
    TXT and CNAME rows, Re-check, Live/Pending) plus a new "Sends from <domain>" line whose
    "receives mail" check I could not confirm the meaning of, so the page does not mention it.
  - The send dialog and Map columns were then re-verified the same day on the DEMO workspace
    (dev preview, wily-bandicoot, two scenario.vy.test people imported under the auto tag,
    nothing sent, send dialog cancelled). Send dialog observed: "Send to 2 people? / Each
    person gets a verification link locked to their address", Recipients, From (with no
    confirmed sender it reads that none is confirmed yet and offers Add a sender, so sending
    fails closed), Message with the Initial invite dropdown, Edit and a copy preview, then
    Cancel / Send to N. Senders add form observed: From address, Display name, Reply-to
    (optional), and the helper "We email the address a confirmation link. Nothing sends from
    it until someone clicks through" - now stated in step 4. The fresh-workspace Activity tab
    offers "Add a sender" in a banner instead of the address chip; step 4 covers both entries.
  - The "Add" button label: verified on PROD by staging one example.com row in the ineffable-
    dormouse paste box and clearing it (nothing imported). Empty box reads "Add people";
    with a list staged it reads "Add 1"/"Add 2" with a "N people ready to add" count.
  - DEV-ONLY surfaces seen on the demo preview and deliberately NOT documented (prod, checked
    the same day, lacks them): the TAG cohort dropdown and Tags panel on Integration ("Sep 17
    upload" default), the post-mapping "Add N people?" summary dialog with BY EMAIL / NEW /
    UPDATED counts, and the live-check configuration lock (Switch method on a LIVE check
    offers "Duplicate this check" on dev; prod still opens the normal dialog, whose copy
    says you can switch back any time). When these ship, step 2 and step 3 need a pass, and
    the Add people screenshot goes stale.
  - Phone surfaces exist now (the method's SETUP chip reads "Email or phone", the Activity
    search placeholder reads "email or phone") but the import panel still speaks email only.
    This page stays scoped to email until someone walks the phone path.

SCREENSHOTS STILL PENDING. The three commissioned shots for this page are named in
images/dashboard/README.md (existing-users-add-people, senders-panel, send-dialog), demo
workspace only. The demo preview sits behind a staff Google sign-in as of 2026-09-17, so they
wait until someone signs in and captures them. Until then switch-method.jpg is the page's only
image.
*/}

Verify people you already have: import your list, link the address your invites leave from,
and send each person a link locked to their email. If you do not have a workspace yet,
[the Start guide](/docs/get-started/getting-access) covers getting access.

<Steps>
  <Step title="Create your verification">
    In your [VerifyYou dashboard](https://platform.verifyyou.com/dashboard/verifications), if you already have a verification you want to use, skip to step 2.
    1. Click **New verification** and pick your check. [Creating a verification](/docs/verifications/creating-a-verification) walks the wizard, and [Configuration](/docs/verifications/configuration) explains each setting.
    2. Choose **No code** when the wizard asks how the check will reach people.
    3. Add your logo and company name so the check carries your brand. [Branding your check](/docs/verifications/branding-your-check) shows what each part controls.
    4. Continue through to **Integration**.

    <p className="vy-have"><strong>You now have:</strong> a live verification, branded as yours.</p>
  </Step>
  <Step title="Switch the method to Existing users">
    On the verification's **Integration** tab.
    1. Click **Switch method** and choose **Existing users**.
    2. Set where people land after they verify, under **Lands on**, with **Edit**. If you leave it unset, people see the default finish screen.

    <p className="vy-have"><strong>You now have:</strong> the import view, ready to take your list.</p>

    <Frame caption="Switch method: pick Existing users. You can switch back any time.">
      <img style={{ maxWidth: "440px", width: "100%", display: "block", margin: "0 auto" }} src="/images/dashboard/switch-method.jpg" alt="Switch method dialog with Existing users selected" />
    </Frame>
  </Step>
  <Step title="Add your people">
    In the **Add people** panel on the same tab.
    1. Paste your list, or click **Upload CSV**. Whole spreadsheet rows are fine, with or without a header. The panel reads your list and tells you how many people are ready to add.
    2. Click **Add**, which carries the count of people you are adding. **Map columns** opens before anything comes in.
    3. Check the four fields at the top: **Email address**, then **IP address**, **Impact figure** and **Date for the figure**, each marked optional. We match your columns to them automatically, and each match is a dropdown, so if we guessed wrong you point it at the right column yourself. Only email is required, and the import runs with the others unmapped.
    4. Everything else in your file sits under **Extra data**, saved to each person's record with a type on each column: currency, date, number, text. Tick a column under **Select columns to leave out** to skip it this time. Import whatever you would want to compare later. An IP address you hold comes back beside the one we see when that person verifies.
    5. Check the count at the foot of the dialog, importing so many critical fields and so many extra columns, then click **Confirm mapping**. Rows with no email are skipped and counted, and empty cells stay empty rather than being filled in for you.

    Each person you add gets a verification link locked to their address. Adding people never sends anything, so you can build the list over several sittings and send once, when you are ready.

    <p className="vy-have"><strong>You now have:</strong> your people imported, each with their own locked link and your extra columns on their record, and nothing sent.</p>
  </Step>
  <Step title="Link the address your invites leave from">
    On the verification's **Activity** tab.
    1. Click the address shown in the header, next to **Send unsent**. On a workspace with no address linked yet, the tab offers **Add a sender** instead. Both open the **Senders** panel.
    2. Add your address with **Add sender**: the From address, a display name, and an optional reply-to. We email the address a confirmation link, and nothing sends from it until someone clicks through. When it reads **Confirmed. Ready to send**, you can send.
    3. To send from your own domain, publish the TXT and CNAME records shown under **Your own domain** with your DNS provider, then click **Re-check**. Each record flips to **Live** once it is seen. You can send before the records are live, and having them in place keeps your invites out of spam, so plan this in before a large batch.

    <p className="vy-have"><strong>You now have:</strong> a confirmed From address your invites will leave from.</p>
  </Step>
  <Step title="Set up your message, send, and watch the results come in">
    On the **Activity** tab, with your people listed.
    1. Click **Message** in the header to set up what your invites say. Two templates live here: **Initial invite** for people who have not verified yet, and **Re-verification** for people you are inviting back.
    2. Edit the subject and the body until they read as yours, then save. **Verification link** drops the link into the copy, and each send turns it into the personal locked link of whoever receives it. Saved changes apply to future sends, not to invites already out.
    3. Select the people to invite, or use **Send unsent** to reach everyone you have not contacted yet. The button shows how many people that is.
    4. Click **Send**, check the dialog, and confirm. It shows who is getting the invite, the address it leaves from, and the message, with one more chance to edit it.

    We host the sending, so there is no mail system on your side to wire up. The **Email invites** view then tracks each invite through sent, opened, clicked and verified as people come through, so you can see who opened and stopped and who never opened at all, and [Reading Activity](/docs/activity/reading-activity) covers what to do with what arrives.

    <p className="vy-have"><strong>You now have:</strong> invites out, and results arriving on Activity as each person verifies. That's the whole setup.</p>
  </Step>
</Steps>

<Note>
**Beta note.** Existing users is in beta. Hosted sending also has a daily cap, and the counter on the Activity header shows where you stand against it today.
</Note>

## What happens next

<div className="grid md:grid-cols-2 gap-5 mt-6">
  <a href="/docs/activity/reading-activity" className="vy-card bg-gray-50 dark:bg-zinc-900 border border-gray-200 dark:border-zinc-800 text-gray-900 dark:text-zinc-50">
    <span className="vy-card-wm"><Icon icon="list-check" iconType="light" size={250} /></span>
    <span className="vy-card-icon"><Icon icon="list-check" iconType="light" size={22} /></span>
    <span className="vy-card-title">Reading Activity</span>
    <span className="vy-card-sub text-gray-500 dark:text-zinc-400">The record of everyone who completed a check.</span>
  </a>
  <a href="/docs/use-cases/userbase-fraud-audit" className="vy-card bg-gray-50 dark:bg-zinc-900 border border-gray-200 dark:border-zinc-800 text-gray-900 dark:text-zinc-50">
    <span className="vy-card-wm"><Icon icon="user-check" iconType="light" size={250} /></span>
    <span className="vy-card-icon"><Icon icon="user-check" iconType="light" size={22} /></span>
    <span className="vy-card-title">Userbase healthcheck</span>
    <span className="vy-card-sub text-gray-500 dark:text-zinc-400">The use case this method was built for.</span>
  </a>
</div>
